/** Static identity for the app. Keep in step with package.json / Cargo.toml. */
export const APP = {
  name: "Thomsen VM",
  /** Sidebar wordmark. */
  brand: "Thomsen VM",
  version: "0.1.0",
} as const;
